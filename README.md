# Installation
1. Install [YADM](https://yadm.io/docs/install)
2. `yadm clone --bootstrap https://github.com/jasonboukheir/dotfiles.git`
3. `sudo sh -c "echo $(which zsh) >> /etc/shells" && chsh -s $(which zsh)`
